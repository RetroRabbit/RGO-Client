import { createAction, props } from '@ngrx/store';

export const GetAllWorkshops = createAction('[workshop] GetAllWorkshops');
export const GetAllWorkshopsSuccess = createAction('[workshop] Get Workshop Success');
